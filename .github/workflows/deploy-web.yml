name: Deploy web app to GitHub Pages

on:
  push:
    branches: [main, dev]
    paths:
      - 'web/**'
      - '.github/workflows/deploy-web.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:proceed? → Push everything except the workflow file for now (Recommended)

  Pushed to dev, ran 1 shell command

Pages is now configured with GitHub Actions as the build source (build_type: workflow, html_url: https://duckmahn.github.io/hand-gesture-portal-filter/). All commits except the workflow file are pushed to origin/dev.

Status: everything is deployed exceptlf, which needs the workflow OAuthscope to push. Two options to finish:

⏺ User answered Claude's questions:
· How do you want to get the workflow file onto GitHub so Pages actually builds and deploys? → I'll add it via github.com's web UI (Recommended)

Here's how to add it:

1. Go to https://github.com/duckmahn/hand-gesture-portal-filter/new/dev?filename=.github/workflows/deploy-web.yml (this pre-fills the path and branch — should open GitHub's "create new file" editor directly on dev)
2. Paste the content below into the editor
3. Scroll down and commit directly to the dev branch

name: Deploy web app to GitHub Pages

on:
  push:
    branches: [main, dev]
    paths:
      - 'web/**'
      - '.github/workflows/deploy-web.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: web/package-lock.json
      - run: npm ci
        working-directory: web
      - run: npm test
        working-directory: web
      - run: npm run build
        working-directory: web
      - uses: actions/upload-pages-artifact@v3
        with:
          path: web/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
